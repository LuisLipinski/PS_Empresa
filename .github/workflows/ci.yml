name: CI - Unit Tests

on:
  push:
    branches:
      - 'feature/**'

jobs:
  unit-tests:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Permissão Maven
        run: chmod +x ./mvnw

      - name: Rodar unitários
        run: ./mvnw clean test
